# secrets — never commit
.env
.env.*

# node
node_modules/
web/node_modules/

# go build artifacts (binary at repo root only — do NOT ignore cmd/kubagachi/,
# which is the main package source)
/kubagachi
*.test
*.out
*.prof

# editor / OS
.DS_Store
*.swp
.mission/
